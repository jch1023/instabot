
export const metadata = {
    title: '로그인 - Instagram Bot',
};

export default function LoginLayout({ children }) {
    return (
        <div className="bg-gray-100 min-h-screen">
            {children}
        </div>
    );
}
